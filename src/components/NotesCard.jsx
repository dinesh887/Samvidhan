import { ArrowRight } from 'lucide-react'
import PremiumBadge from './PremiumBadge'
import { useLanguage } from '../context/LanguageContext'
export default function NotesCard({ note, onOpen }) { const { pick, t } = useLanguage(); return <article className="flex flex-col rounded-2xl border border-navy/10 bg-white/60 p-5 dark:border-ink-dark/10 dark:bg-white/[0.04]"><div className="flex items-start justify-between gap-3"><span className="text-xs text-saffron">{note.category}</span><PremiumBadge /></div><h2 className="font-display mt-4 text-lg font-semibold text-navy dark:text-ink-dark">{pick(note.title)}</h2><p className="mt-2 flex-1 text-sm leading-relaxed text-ink/60 dark:text-ink-dark/60">{pick(note.description)}</p><p className="mt-4 text-xs text-ink/45 dark:text-ink-dark/45">{note.language}</p><button type="button" onClick={onOpen} className="mt-5 inline-flex items-center gap-2 text-sm font-medium text-saffron">{t('common_open_note')} <ArrowRight size={15} /></button></article> }