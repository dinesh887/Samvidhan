import { useEffect } from 'react'
import ExamCard from '../components/ExamCard'
import RecommendedBooks from '../components/RecommendedBooks'
import { books } from '../data/books'
import { useLanguage } from '../context/LanguageContext'
const exams = [{ code: 'MPSC', title: 'MPSC Constitution Preparation', description: 'A practical path from foundational Articles to Maharashtra-focused revision.', features: ['Important Articles', 'Previous year questions', 'Daily MCQ', 'Topic-wise quiz', 'Quick revision', 'Important Amendments'] }, { code: 'UPSC', title: 'UPSC Polity Preparation', description: 'Build conceptual depth and practise the reasoning expected in civil services exams.', features: ['Important Articles', 'Previous year questions', 'Advanced MCQ', 'Topic-wise practice', 'Revision notes'] }]
export default function ExamPreparation() { const { t } = useLanguage(); const localizedExams = exams.map((exam) => ({ ...exam, title: t(exam.code === 'MPSC' ? 'exam_mpsc' : 'exam_upsc'), description: t(exam.code === 'MPSC' ? 'exam_mpsc_desc' : 'exam_upsc_desc') })); useEffect(() => { document.title = `${t('exam_title')} | Samvidhan` }, [t]); return <div><div className="mx-auto max-w-7xl px-4 py-14 sm:px-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-saffron">{t('common_study_tracks')}</p><h1 className="font-display mt-3 text-4xl font-semibold text-navy dark:text-ink-dark">{t('exam_title')}</h1><p className="mt-3 max-w-2xl text-ink/60 dark:text-ink-dark/60">{t('exam_sub')}</p><div className="mt-10 grid gap-5 md:grid-cols-2">{localizedExams.map((exam) => <ExamCard key={exam.code} exam={exam} />)}</div></div><RecommendedBooks books={books} /></div> }